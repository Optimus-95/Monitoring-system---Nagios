# Nginx Configuration

## Create Configuration File

```bash
sudo vi /etc/nginx/conf.d/nginx_ws.conf
```
Add Nginx Configuration
( configuration file is provided under configs/nginx_ws.conf )

# Reload Nginx
```bash
sudo nginx -s reload
```
